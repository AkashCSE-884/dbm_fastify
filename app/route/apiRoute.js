module.exports = async(fastify) => {
    // fastify.register(require('../middleware/jwtMiddleware'));

    // fastify.get('/protected', { preValidation: [fastify.authenticate] }, async(request, reply) => {
    //     reply.send({ message: 'This is a protected route' });
    // });
};